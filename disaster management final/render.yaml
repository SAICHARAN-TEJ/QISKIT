services:
  - type: web
    name: resqbit
    env: python
    rootDirectory: disaster management final
    buildCommand: pip install -r requirements.txt
    startCommand: cd webapp && python app.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
